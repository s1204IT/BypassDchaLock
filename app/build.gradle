apply plugin: 'com.android.application'

android {
    compileSdk 22

    defaultConfig {
        minSdk 22
        //noinspection ExpiredTargetSdkVersion
        targetSdk 22
        versionCode 16
        versionName "1.6.4"
    }
    buildTypes {
        debug {
            minifyEnabled true
        }
        release {
            minifyEnabled true
        }
    }
    namespace 'jp.co.benesse.touch.setuplogin'
}

dependencies {
    implementation files('libs/IBenesseExtensionLibrary.jar')
}
